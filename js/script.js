const remote = require('electron').remote;
const {dialog} = require('electron').remote;

Vue.use(Vuex);

const store = new Vuex.Store({
	state: {
		
	},
	mutations: {

	}
});

const app = new Vue({
	el: '#main-app'
});